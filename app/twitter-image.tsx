// Twitter card image — same artwork as the OpenGraph image.
// Route segment config can't be re-exported, so `runtime` is declared here.
export const runtime = 'edge'

export { default, alt, size, contentType } from './opengraph-image'
